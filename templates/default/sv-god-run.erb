#!/bin/bash

exec 2>&1

if [ -s "/usr/local/rvm/environments/default" ];then
  source "/usr/local/rvm/environments/default"
fi

trap 'kill -HUP %1' 1 2 13 15

exec /usr/bin/env god -D -c /etc/god/master.god --no-syslog & wait
